Fragment frag(Figure noteLa, Figure noteRe) {
    :noteLa :noteRe
 }

void main() {
    Figure noteLa = La.n;
    Figure noteRe = Re.n;
    noteLa = noteRe;
    || for(int i = 0; i < 2; ++i) {F:frag(noteLa, noteRe);}||
}
